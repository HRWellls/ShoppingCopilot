# Shopping Copilot 终局评分优化方案

## 1. 目标与判断

当前 Hit Rate@10、MRR、MTTC 已经接近优化上限。公开集已有较强结果（Hit Rate@10 0.9600、MRR 0.606629、MTTC 2.585、TechnicalScore 0.830289、p95 68.675 ms），继续投入检索微调的边际收益有限。

最终目标应从“指标更高”转为“评委相信这是一套可靠、可解释、可复现、能落地的产品”。重点覆盖：

- Technical Execution（35%）：稳定性、工程证据、模型/API 使用边界；
- Innovation & Problem Insight（20%）：问题定义和方案差异化；
- Impact & Relevance（20%）：真实用户和电商业务价值；
- Feasibility & Practicality（15%）：成本、资源、上线路径；
- Presentation & Communication（10%）：Demo 叙事和答辩能力。

## 2. 优先级工作包

### P0：交付可靠性与现场 Demo

**要解决的问题：**现场运行是否稳定，是否能证明系统不是为几个样例定制的。

**工作内容：**

1. 固化一条离线可运行路径：无 API Key、无网络、无 dense 模型时仍能完成规则 + BM25 响应。
2. 增加启动自检：目录、索引、配置、Python 依赖和版本检查；失败时给出可读错误。
3. 准备四段固定演示：Buying 多轮、品牌覆盖/清除、Browsing 场景、主动澄清。
4. 录制前执行完整测试和 smoke test，准备预生成结果作为现场备份。
5. 记录并展示 contract failure、session leakage、异常 fallback、第 10 轮保护和 p95 延迟。

**验收标准：**连续运行至少 20 个会话无崩溃；每轮最多 10 个合法去重 ASIN；模型不可用时仍能返回结果；Demo 可在干净环境按 README 启动。

### P1：工程证据与可复现性

**要解决的问题：**评委能否看懂架构，并相信结果可重复。

**工作内容：**

- 输出一页架构图：官方接口 → 状态 → 意图/槽位 → Buying/Browsing 检索 → 重排 → 校验 → 澄清/推荐 → trace。
- 固定依赖、配置版本、索引 manifest、数据 checksum 和运行命令。
- 增加分层评测：Buying、Browsing、Intent Override、Boundary；同时保留 ablation（去澄清、去 relaxation、去 dense/LLM）。
- 提供 p50/p95 延迟、启动时间、内存占用、索引构建时间和失败率。
- 对同义表达、拼写错误、空结果、价格缺失、否定和重复 reset 做回放测试。

**验收标准：**任何成员可用一条命令复现核心评测；报告中明确公开集、盲测集和实验配置，避免只展示最好的一组数字。

### P1：业务价值与影响力

**要解决的问题：**高分如何转化为真实电商收益。

**工作内容：**

- 将指标翻译为用户结果：更少搜索轮次、更早看到可行动商品、降低因筛选困难导致的放弃。
- 为每个推荐项展示标题、价格和基于目录事实的匹配原因，增强信任。
- 设计线上指标：搜索到点击、加购/转化、澄清回答率、无结果率、人工客服转接率。
- 明确可接入的业务系统：商品详情、库存/配送、用户偏好和 A/B 测试；说明当前原型暂不包含这些模块。

**建议答辩表述：**“我们把一次性关键词搜索变成可修正的购物决策过程；系统在保持硬约束的同时，用更少轮次把用户带到可信候选。”

### P1：可行性、成本与风险

**要解决的问题：**原型是否能在真实资源约束下运行。

**工作内容：**

- 说明单进程、只读目录、驻内存索引和 CPU fallback 的资源模型。
- 估算单会话成本：规则路径成本、可选 LLM 调用次数、超时预算和缓存策略。
- 明确模型边界：模型只做 schema-bounded 意图/槽位解析，不能直接选择 ASIN 或绕过硬约束。
- 补充风险表：目录陈旧、库存变化、错误解析、隐私、品牌/价格事实和模型不可用时的降级策略。
- 给出生产路线：离线索引构建 → 灰度/A-B → 线上 trace 与告警 → 目录和模型版本回滚。

**验收标准：**文档能回答“没有 API 时怎么办”“50 万/500 万商品如何扩展”“延迟和成本如何控制”“错误推荐如何回滚”。

### P2：创新与表达

统一使用三点创新叙事：

1. Buying / Browsing 双轨：购买重硬约束和精度，浏览重语义和多样性；
2. 槽位级状态：用户改口只覆盖受影响条件，不污染整个历史；
3. 信息增益澄清：问题本身是检索动作，且第 10 轮必须给出结果。

避免把项目描述成“换了更大的模型”或“只调高了检索分数”。

## 3. 角色分工：现在开始做什么

### 开发者

**今天开始：**

1. 冻结当前高分配置、索引和公开集基线，生成一份带时间、commit、配置版本的结果 JSON。
2. 跑完整测试、分层评测和 ablation，补齐失败率、session isolation、p50/p95、启动时间和内存数据。
3. 编写启动自检和离线 fallback 的现场脚本；确认无 API Key 也能完成四段 Demo。
4. 为边界条件补测试：空结果、价格缺失、否定、override、重复 reset、非法输入、turn=10。
5. 输出“架构与可靠性”一页材料，并把所有复现命令整理进 README。

**本阶段交付物：**

- 可复现的 benchmark JSON/摘要；
- 一键启动和一键评测命令；
- 稳定性/边界测试报告；
- 资源与成本表；
- 架构图、fallback 流程图、版本与回滚说明。

### 产品 / Demo 负责人

**今天开始：**

1. 写好 4～6 分钟 Demo 脚本：问题定义（30 秒）、架构（50 秒）、Buying、Browsing、澄清、指标和收尾。
2. 为四个场景准备固定输入、预期状态变化、关键画面和一句业务解释；每个场景开始前明确 `reset`。
3. 把技术指标改写成用户价值和业务价值，准备“为什么不是普通聊天推荐”的回答。
4. 制作四页以内材料：产品定位、架构闭环、指标/成本、落地路线与限制。
5. 录制一版无旁白彩排和一版完整视频；检查字幕、终端字号、链接、版权和敏感信息。
6. 准备答辩问答：模型为何可选、API 失败怎么办、如何扩展到更大目录、如何验证真实转化、当前局限是什么。

**本阶段交付物：**

- 固定 Demo 脚本和录屏文件；
- 一页业务价值/用户旅程；
- 评委版指标摘要；
- FAQ 与 60 秒项目介绍；
- Devpost/README 的产品描述和公开视频链接。

## 4. 最终检查清单

- [ ] 四段 Demo 在干净环境可重复运行。
- [ ] 无 API、无 dense 模型时仍有合法响应。
- [ ] 公开集、盲测集、分层指标和 ablation 已冻结。
- [ ] 展示准确率之外的延迟、稳定性、成本和资源数据。
- [ ] README 包含安装、复现、限制、未来工作和团队贡献。
- [ ] 视频不包含 API Key、私有标签、内部路径或未授权素材。
- [ ] 每位成员都能解释状态机、检索路径、降级机制和核心业务价值。

## 5. 时间安排建议

**第 1 天：**开发者冻结基线并补工程数据；产品负责人冻结脚本和四个场景。

**第 2 天：**完成边界测试、启动自检、材料初稿和第一次录屏。

**第 3 天：**全员模拟答辩，修复 Demo 风险，冻结代码、配置、视频和提交文案。

原则是：代码只接受影响可靠性和可解释性的必要修改；检索主指标保持冻结，避免临近提交引入不可控回归。
