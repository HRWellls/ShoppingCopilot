# Shopping Copilot 技术文档

## 1. 技术目标与约束

本方案采用 **FSM-Hybrid（显式有限状态机 + 混合检索）** 作为 Agent 内核。目标是在官方 `reset/respond` 接口和最多 10 轮会话约束下，提高真实购买商品的召回和排序，同时控制对话轮数。

### 1.1 已知约束

- 目录：约 50,000 个 Amazon Reviews 2023 `Clothing_Shoes_and_Jewelry` 商品，静态只读。
- 评测：200 个公开开发会话，800 个私有会话；会话彼此隔离。
- 响应：每轮最多返回 10 个 `parent_asin`，第 10 轮仍需返回合法结果。
- 运行：Python 单进程，索引全部驻内存；不依赖必须联网的模型或服务。
- 范围：文本输入，不做 UI、多模态、目录写入、外部向量数据库和基础模型全参数训练。
- 优化目标：官方 evaluator 输出的 Hit Rate@10、MRR、MTTC、Efficiency、TechnicalScore；题目提供的组合目标为 `0.50*Hit@10 + 0.30*MRR + 0.20*Efficiency`。

### 1.2 设计原则

1. **状态机优先于自由 Agent 循环**：所有动作来自有限状态和 typed 工具，保证可复现。
2. **召回优先于重排**：先保留足够宽的候选池，再在 Top 30～50 内做昂贵排序。
3. **硬约束与软偏好分离**：硬约束用于过滤，软偏好用于排序；绝不让模型绕过硬约束。
4. **可选模型可失败**：结构化校验、超时和本地 fallback 是必需路径。
5. **每次实验可回放**：记录配置、代码版本、索引版本和逐轮事件。

## 2. 总体架构

```text
official reset/respond adapter
             │
             ▼
       SessionStateStore
             │
message ─► IntentRouter + SlotExtractor
             │
             ▼
       OverrideResolver
             │
       FSM Dispatcher
        ┌────┴────┐
        ▼         ▼
   BuyingTrack  BrowsingTrack
        │         │
        └────┬────┘
             ▼
    Candidate Fusion (RRF/weighted)
             ▼
      Top 30~50 Reranker
             ▼
       Hard Constraint Verifier
             ▼
      Dialogue Policy
       ┌─────┴─────┐
       ▼           ▼
  Recommend     Clarify/Relax
       │           │
       └─────┬─────┘
             ▼
 TraceRecorder + official response adapter
```

### 2.1 进程级组件

- `CatalogStore`：规范化商品表和 `parent_asin -> Product` 映射。
- `LexicalIndex`：BM25 或 starter agent 已提供的内存词法索引。
- `DenseIndex`：NumPy/FAISS `IndexFlatIP`；没有本地模型时禁用并保留 BM25 路径。
- `Reranker`：规则分数、可选 Cross-Encoder、可选低温 LLM 的组合。
- `AgentConfig`：所有权重、阈值、开关和版本号集中管理。

### 2.2 会话级组件

- `SessionState`：当前意图、槽位、历史、候选、轮数和最近一次动作。
- `FSMState`：当前状态机节点。
- `ProfileDistiller`：只维护当前会话摘要接口；当前评测不做跨会话持久化。
- `TraceRecorder`：记录可回放的事件，不保存私有数据到公开仓库。

## 3. FSM 设计

### 3.1 状态枚举

```python
class FSMState(str, Enum):
    START = "start"
    UNDERSTAND = "understand"
    RETRIEVE = "retrieve"
    ASSESS = "assess"
    CLARIFY = "clarify"
    RELAX = "relax"
    RECOMMEND = "recommend"
    LIMIT_RECOMMEND = "limit_recommend"
    ERROR_FALLBACK = "error_fallback"
```

### 3.2 状态转移

| 当前状态 | 条件 | 下一状态 | 动作 |
|---|---|---|---|
| `START` | 首条消息到达 | `UNDERSTAND` | 初始化会话并解析消息 |
| `UNDERSTAND` | 解析成功 | `RETRIEVE` | 合并槽位、处理 override |
| `UNDERSTAND` | 解析失败 | `ERROR_FALLBACK` | 使用规则/空状态查询 |
| `RETRIEVE` | 有效槽位或语义 query | `ASSESS` | 双轨召回、融合、去重 |
| `ASSESS` | 候选稳定且置信度高 | `RECOMMEND` | 校验并生成 Top 10 |
| `ASSESS` | 候选过宽/熵高 | `CLARIFY` | 选择信息增益最高的槽位 |
| `ASSESS` | Buying 空集 | `RELAX` | 按优先级放宽一个约束后重检索 |
| `RELAX` | 放宽后仍空集 | `CLARIFY` | 询问是否接受放宽或替代品类 |
| 任意 | `turn_count >= 10` | `LIMIT_RECOMMEND` | 停止追问，返回当前最佳候选 |
| 任意 | 未捕获异常 | `ERROR_FALLBACK` | 记录错误并返回确定性结果 |

状态机不得依赖自由文本中的隐式循环；一次 `respond` 最多完成一次解析、一次检索/重排和一次策略决策。低置信度 Verifier 最多触发一次，不允许递归反思。

## 4. 数据模型

### 4.1 商品模型

```python
@dataclass(frozen=True)
class Product:
    parent_asin: str
    title: str
    category: tuple[str, ...]
    brand: str | None
    price: float | None
    features: tuple[str, ...]
    description: str
    searchable_text: str
    metadata: dict[str, str]
```

`searchable_text` 建议按字段拼接并加权：标题和品类重复 2～3 次，品牌和 features 重复 1～2 次，description 截断到固定字符数。原始字段保留用于过滤和展示。

### 4.2 槽位模型

```python
class SlotKind(str, Enum):
    HARD = "hard"
    SOFT = "soft"
    CONTEXT = "context"

@dataclass
class SlotValue:
    value: Any | None
    kind: SlotKind
    confidence: float
    source: str                 # rule / model / user-confirmed
    turn_seen: int
    ttl: int | None              # HARD 槽位为 None
    negated: bool = False
```

推荐槽位：`category`、`brand`、`price_min`、`price_max`、`color`、`size`、`material`、`occasion`、`gender`、`style`、`exclusions`。其中预算、尺码和用户明确的不可接受条件默认为 `HARD`；场景和风格默认为 `CONTEXT` 或 `SOFT`。

### 4.3 会话模型

```python
@dataclass
class SessionState:
    session_id: str
    turn_count: int = 0
    fsm_state: FSMState = FSMState.START
    intent: str = "unknown"          # buying / browsing / unknown
    intent_confidence: float = 0.0
    slots: dict[str, SlotValue] = field(default_factory=dict)
    history: list[dict[str, str]] = field(default_factory=list)
    candidate_ids: list[str] = field(default_factory=list)
    last_query: str = ""
    last_action: str | None = None
    asked_slots: set[str] = field(default_factory=set)
    relaxation_level: int = 0
```

### 4.4 解析结果模型

```python
@dataclass
class ParsedTurn:
    intent: Literal["buying", "browsing", "unknown"]
    intent_confidence: float
    slot_updates: dict[str, SlotValue]
    clears: set[str]
    overrides: set[str]
    query_text: str
    evidence: list[str]
```

模型或规则输出必须先通过 schema 校验，再进入状态更新；非法字段直接丢弃并记录 `parse_error`。

## 5. `reset/respond` 适配层

官方 participant kit 的确切参数和返回结构以其 API contract 为准。内部建议保持以下职责分离：

```python
def reset(session_id: str | None = None) -> None:
    """创建全新 SessionState；不得复用上一会话槽位或候选。"""

def respond(message: str) -> OfficialResponse:
    """将官方输入映射到 FSM，返回协议要求的文本/ASIN 结构。"""
```

适配器负责：

1. 校验输入类型和长度。
2. 维护当前 `session_id` 与轮数。
3. 将内部 `RecommendationResponse` 转为官方响应。
4. 确保 ASIN 合法、去重、最多 10 个。
5. 捕获所有异常并调用 `ERROR_FALLBACK`，不把堆栈返回给 evaluator。

## 6. 意图和槽位抽取

### 6.1 三层 fallback

```text
规则先验
   ↓ 低置信度或复杂表达
本地小模型/embedding 分类器
   ↓ 仍低置信度且已配置
外部 LLM 结构化抽取
   ↓ 超时、限流、非法 JSON
规则结果或上一轮稳定状态
```

LLM 不能直接选择 ASIN、调用任意工具或修改未声明字段。

### 6.2 规则信号

- Buying：`buy / need / looking for / under $X / size / color / brand` 等。
- Browsing：`recommend / ideas / what should I wear / options / something for` 等。
- Override：`actually / instead / change / any brand is fine / no longer / remove` 等。
- 否定：`without / no / don't want / not ...`，写入 `exclusions` 或清除对应槽位。

规则匹配先做单位、颜色、尺码和价格归一化，再计算置信度；不要用单个词决定路由。

### 6.3 OverrideResolver

处理顺序必须是：

1. 识别明确的新值、清除意图和否定范围。
2. 对同一槽位执行 `clear -> set`，新值覆盖旧值。
3. 重新生成当前查询，不把历史文本简单拼接。
4. Buying 硬约束若被改写，清空旧候选并重新检索。
5. 未明确覆盖的硬约束保留；软偏好可按 TTL 衰减。

示例：`black -> white` 得到 `color=white`；`Nike -> any brand` 得到 `brand=None`；`under $100 -> under $80` 更新 `price_max=80`。

## 7. 目录预处理与驻内存索引

### 7.1 预处理流程

```text
读取目录 → 字段类型校验 → 文本清洗/同义词归一化
→ 生成 Product → 生成 searchable_text
→ 建立 BM25 → 批量生成 embedding → 建立向量索引
→ 保存索引元数据和版本哈希
```

预处理只在启动或显式 build 命令执行，不在每轮会话中重复。

### 7.2 词法索引

优先使用 starter agent 已有实现；否则使用 `rank_bm25` 或等价内存 FTS。对标题、品类和品牌增加字段权重。BM25 返回原始分数后，在当前候选集合内做 min-max 或 z-score 归一化，避免与向量分数直接相加造成尺度偏差。

### 7.3 Dense 索引

优先 `sentence-transformers` 的轻量模型（如 bge-small-en-v1.5；无网络时使用已缓存的 MiniLM）。向量归一化后使用内积相似度；FAISS 不可用时用 NumPy 分块矩阵乘法。索引必须支持 top-k、版本查询和关闭开关。

### 7.4 Query 构造

```text
query = category + brand + color + size + material + occasion + style + exclusions
```

Buying 将硬约束放入结构化 filter，不仅放进文本 query；Browsing 保留场景和风格语义，并限制 description 长度，降低噪声。

## 8. 双轨检索实现

### 8.1 Buying Track

1. 从 `SessionState` 生成硬过滤表达式：品类、价格、尺码、明确品牌和排除项。
2. 在过滤后的集合上执行 BM25，召回 `lexical_k`。
3. 在全目录或过滤集合上执行 dense，召回 `dense_k`，用于补召回。
4. 加入品类/元数据匹配分和当前会话软偏好分。
5. 若过滤结果为空，按顺序放宽：`brand → color/material → category synonym → price`；预算硬约束默认不自动放宽，先询问用户。

### 8.2 Browsing Track

1. 以场景、品类和风格组成语义 query，dense 召回为主。
2. BM25 补充专有名词、品牌和精确颜色等 lexical 信号。
3. 依据品类、品牌或风格做轻量分桶，保证 Top 10 不被同一商品族占满。
4. 软偏好只影响排序，不淘汰所有不匹配项。

### 8.3 初始融合权重

权重是公开集实验起点，必须可配置：

| 轨道 | lexical/filter | dense | category | profile/soft |
|---|---:|---:|---:|---:|
| Buying | 0.45 | 0.25 | 0.20 | 0.10 |
| Browsing | 0.30 | 0.45 | 0.15 | 0.10 |

优先采用 Reciprocal Rank Fusion：

```text
RRF(d) = Σ_i weight_i / (k_rrf + rank_i(d))
```

默认 `k_rrf=60`。如使用加权分数，先在每个召回源内部归一化，再加权求和。

### 8.4 候选池大小

召回阶段保持宽候选，建议 lexical/dense 各 100～500，融合后保留 100～200；重排处理 Top 30～50，最终返回 Top 10。候选池不得在第一轮过早截断到 10，否则后续轮次无法挽回召回失败。

## 9. 重排与校验

### 9.1 分层重排

```text
融合 Top 100~200
       ↓
规则/元数据预排序 Top 50
       ↓
Cross-Encoder（可选）Top 20
       ↓
低温 LLM（可选、最多一次）或确定性最终排序
       ↓
Hard-constraint Verifier
       ↓
Top 10
```

### 9.2 规则分数

```text
score =
  w_lex * lexical_norm
  + w_dense * dense_norm
  + w_category * category_match
  + w_soft * soft_preference_match
  + w_profile * session_profile_match
  - w_penalty * soft_conflict
```

硬约束不满足时直接丢弃或赋极大负值；不允许通过语义相关性抵消预算、尺码或明确排除条件。

### 9.3 Cross-Encoder/LLM 边界

- Cross-Encoder 只处理 Top 30～50，缓存 `(query, parent_asin, model_version)`。
- LLM 只接收 Top 20 的结构化摘要，不接收 50,000 个商品。
- LLM 输出必须是候选 ID 的有序列表或 JSON 分数，经过集合校验；缺失、重复、未知 ID 时回退本地排序。
- LLM 超时建议 300～800ms（按设备调参），超过即 fallback；不为等待 LLM 增加对话轮次。

### 9.4 Verifier

Verifier 检查：ASIN 是否存在、价格/尺码/品类等硬条件是否满足、Top 10 是否去重。低置信度时最多执行一次；失败就进入 `CLARIFY` 或本地排序，不重复调用。

## 10. 对话策略与信息增益

### 10.1 决策输入

`candidate_count`、候选分数熵、Top1-Top2 margin、intent_confidence、缺失槽位、turn_count、上次是否已问过该槽位。

候选熵可按归一化分数计算：

```text
p_i = exp(s_i / τ) / Σ_j exp(s_j / τ)
H = -Σ_i p_i log(p_i)
```

### 10.2 初始阈值

阈值全部放入配置并通过公共集分层调参：

- `candidate_count > 100` 或熵高：优先澄清。
- `10 < candidate_count <= 100`：询问一个预期缩减量最大的缺失槽位。
- `candidate_count <= 10` 且 Top1 margin/置信度足够：直接推荐。
- `turn_count >= 8`：只问最关键缺口，同时返回当前候选。
- `turn_count >= 10`：强制推荐，不再询问。

### 10.3 槽位价值估计

对每个缺失槽位 `s` 估计：

```text
value(s) = expected_candidate_reduction(s)
           * slot_confidence
           / (1 + clarification_cost)
```

`expected_candidate_reduction` 可由当前候选的元数据分布估计；没有足够样本时使用离线统计先验。过滤后的候选低于 10 时不再为了理论信息增益追问。

### 10.4 问题生成

优先使用模板，LLM 仅做语言润色：

```text
category: Which type are you looking for: running, casual, or formal?
price: Do you have a maximum budget?
color: Which color would you prefer?
brand: Do you have a preferred brand, or is any brand fine?
```

模板变量必须来自候选分布或合法枚举，防止提出目录不存在的选项。

## 11. Typed 工具边界

内部工具固定为：

| 工具 | 输入 | 输出 | 约束 |
|---|---|---|---|
| `parse_query` | message、history 摘要 | `ParsedTurn` | schema 校验、长度上限 |
| `update_slots` | slots、ParsedTurn | 新 `slots`、变更列表 | 明确覆盖/清除 |
| `filter_catalog` | filters | 商品 ID 列表 | 只读、超时 |
| `search_bm25` | query、k、subset | 排名列表 | k 上限 |
| `search_dense` | query、k、subset | 排名列表 | 可关闭、缓存 |
| `fuse_candidates` | 多路排名 | 候选及分数 | 去重 |
| `rerank` | query、Top N | 排名列表 | N 上限 |
| `verify_recommendations` | slots、候选 | 合法候选 | 不改目录 |
| `ask_attribute` | 缺失槽位、候选分布 | 单个问题 | 每轮一次 |

禁止开放任意 Python 执行、任意文件访问或自由工具循环。

## 12. 单轮处理伪代码

```python
def respond(message: str) -> OfficialResponse:
    state = session_store.current()
    state.turn_count += 1
    state.history.append({"role": "user", "content": message})

    try:
        parsed = parser.parse(message, state)
        state = override_resolver.apply(state, parsed)
        state.intent = route_intent(state, parsed)

        if state.turn_count >= 10:
            state.candidate_ids = retrieve_and_rank(state)
            response = make_recommendation(state, limit=True)
            state.fsm_state = FSMState.LIMIT_RECOMMEND
        else:
            candidates = retrieve_and_rank(state)
            assessment = policy.assess(state, candidates)
            if assessment.action == "relax":
                candidates = retrieve_with_relaxation(state)
                assessment = policy.assess(state, candidates)
            if assessment.action == "clarify":
                response = make_clarification(state, assessment.slot)
                state.fsm_state = FSMState.CLARIFY
            else:
                response = make_recommendation(state)
                state.fsm_state = FSMState.RECOMMEND
            state.candidate_ids = [x.parent_asin for x in candidates]
    except Exception as exc:
        trace.record_error(state, exc)
        response = deterministic_fallback(state, message)
        state.fsm_state = FSMState.ERROR_FALLBACK

    trace.record_turn(state, response)
    session_store.save(state)
    return official_adapter.to_response(response)
```

## 13. 配置建议

建议使用 YAML/TOML，并在每次评测输出完整快照：

```yaml
index:
  lexical_enabled: true
  dense_enabled: true
  embedding_model: "local-model-id"
  index_version: "catalog-sha256:model-version"
retrieval:
  lexical_k: 300
  dense_k: 300
  fused_k: 150
  buying_weights: [0.45, 0.25, 0.20, 0.10]
  browsing_weights: [0.30, 0.45, 0.15, 0.10]
ranking:
  rerank_k: 50
  cross_encoder_enabled: false
  llm_enabled: false
dialogue:
  clarify_count_threshold: 100
  entropy_threshold: 0.70
  min_top1_margin: 0.12
  max_turns: 10
  late_turn: 8
fallback:
  llm_timeout_ms: 600
  verifier_max_calls: 1
```

权重和阈值只是起点；不得在代码中散落 magic number。

## 14. 事件轨迹与可观测性

每轮至少记录以下 JSONL 事件字段：

```json
{
  "session_id": "...",
  "turn": 3,
  "intent": "buying",
  "intent_confidence": 0.91,
  "slot_changes": {"brand": {"old": "Nike", "new": null}},
  "route": "buying",
  "candidate_count": 137,
  "entropy": 0.42,
  "top1_margin": 0.18,
  "asked_slot": null,
  "top10": ["ASIN..."],
  "target_rank": null,
  "latency_ms": 84,
  "fallback": false,
  "config_version": "..."
}
```

生产/提交版本不记录 API key 和私有目标商品；开发环境可在本地保留 evaluator 对齐字段。

## 15. 评测与实验方法

### 15.1 基线与分层

第一份基线必须固定：代码 commit、数据 checksum、依赖版本、索引版本和 evaluator 版本。公共会话按 Buying、Browsing、Override、Boundary 分层报告，不只看总分。

### 15.2 Ablation 顺序

```text
BM25
→ Dense
→ Hybrid
→ Dual-track router
→ Slot state/override
→ Reranker
→ Clarification
→ Information Gain
→ Optional LLM/Verifier
```

每次只改变一个主变量，记录 Hit@10、MRR、MTTC、Efficiency、TechnicalScore、平均延迟和内存。保留验证切分，避免针对单 session 写规则。

### 15.3 错误分类

- `retrieval_miss`：目标未进入宽候选池。
- `hard_filter_miss`：过硬约束或字段解析导致目标被淘汰。
- `intent_error`：Buying/Browsing 路由错误。
- `slot_error`：值、否定或 override 处理错误。
- `ranking_error`：目标在候选中但排序靠后。
- `dialogue_error`：澄清问题价值低或询问过多。
- `boundary_error`：第 10 轮、空集或异常路径不合法。

## 16. 测试计划

### 16.1 单元测试

- 价格、尺码、颜色、品牌、品类解析和归一化。
- `black -> white` 覆盖；`Nike -> any brand` 清除；否定词处理。
- 硬约束过滤、空集渐进放宽和候选去重。
- RRF/加权融合的顺序稳定性。
- 熵、margin 和澄清槽位选择。
- 第 10 轮强制推荐。

### 16.2 集成测试

- `reset` 后会话状态完全为空。
- BM25、dense 关闭时仍可运行。
- LLM 返回非法 JSON、超时或未知 ASIN 时可 fallback。
- 官方 adapter 返回格式、数量和合法 ASIN 均符合 contract。

### 16.3 回放测试

保存固定输入序列和配置快照，验证同一版本在相同环境下输出顺序、状态变更和指标可复现。必要时固定随机种子并禁用非确定性批处理。

### 16.4 发布前检查

```text
[ ] 新环境可安装
[ ] 目录 checksum 正确
[ ] 200 public sessions 可完整运行
[ ] 单 session 不超过 10 turns
[ ] session 之间无状态泄漏
[ ] 无 API key、私有数据和模型缓存进入仓库
[ ] README 能从零复现实验
[ ] Final Candidate 与 Backup Candidate 均已冻结
```

## 17. 性能与资源预算

- 目录索引在启动时构建或加载；禁止每轮重新编码全目录。
- dense 检索优先批量矩阵乘法/FAISS；对重复 query 和 `(query, asin)` 重排结果做缓存。
- LLM 仅在低置信度、Top 20 范围内启用，默认关闭并保留本地路径。
- 通过配置限制 `lexical_k`、`dense_k`、`rerank_k` 和 description 长度，防止内存和延迟失控。
- 记录 p50/p95 延迟、峰值内存和每轮候选数量，作为 TechnicalScore 之外的工程指标。

## 18. 分阶段实现计划

### 阶段 0～1：官方环境

Fork/Clone 官方仓库，安装 Participant Kit，校验目录，跑通 starter agent 和 local evaluator，保存基线。

### 阶段 2：MVP

保留官方接口，完成 `CatalogStore`、`SessionState`、规则路由、BM25、硬过滤、Top 10 和日志。

### 阶段 3：双轨与状态

加入 dense、融合、渐进放宽、结构化槽位、override、候选池管理和基础澄清。

### 阶段 4：精度保护

加入 Top 50 reranker、Verifier 开关、熵/margin 策略和 fallback 测试。

### 阶段 5：实验收敛

按场景做 ablation、调权重和阈值，保留验证切分，冻结最终配置。

### 阶段 6～8：发布

全面测试、干净环境复现、README、公开仓库、Demo、Devpost 和答辩材料。

## 19. 风险与取舍

| 风险 | 原因 | 缓解 |
|---|---|---|
| 过硬过滤损失召回 | 用户表达不精确、目录字段缺失 | 空集检测、只逐项放宽、优先询问预算 |
| dense 语义偏移 | description 噪声、跨类误匹配 | 标题/品类加权、字段截断、同义词归一化 |
| Cross-Encoder CPU 成本 | Top N 过大 | 只重排 30～50、缓存、开关化 |
| LLM 延迟/格式错误 | 外部服务不稳定 | typed schema、超时、规则 fallback |
| 追问导致 MTTC 上升 | 阈值不合理 | 信息增益/成本比、晚轮保护 |
| 公共集过拟合 | 针对个例写规则 | 分层验证、实验审计、禁止 query-specific if |
| 复杂编排漂移 | 自由 planner 或多 Agent 协调 | FSM 主导，仅在低置信局部启用 Planner JSON |

## 20. 可选增强（不阻塞主线）

- 低置信度轮次局部启用 Planner-Executor，输出必须经 schema 和 FSM 校验。
- 单次 Reflective RAG/Verifier 作为保护开关，不做循环反思。
- 用离线 bandit 选择阈值，不进行在线探索。
- 轻量 StateGraph 表达状态边，但不引入多 Agent 专家投票。
- 跨会话画像只保留接口和摘要格式，待评测边界允许后再实现。

## 21. 实现完成定义（Definition of Done）

```text
[ ] 官方 reset/respond 与 JSON contract 全部通过
[ ] Catalog、BM25、dense 索引可独立开关并记录版本
[ ] Buying/Browsing 路由和低置信 fallback 完成
[ ] 槽位新增、覆盖、清除、否定、TTL 规则有测试
[ ] 硬过滤、空集放宽、合法 ASIN 校验完成
[ ] Top 30~50 重排和单次 Verifier 完成
[ ] 候选熵/信息增益澄清每轮最多一个问题
[ ] 第 10 轮始终返回推荐
[ ] 逐轮 trace、回放和分层指标可用
[ ] 无外部模型时仍可复现运行
[ ] 干净环境可按 README 完成安装和评测
[ ] 不提交 API key、私有评测数据和不可恢复缓存
```

## 22. 推荐代码结构与模块职责

在不破坏官方 starter agent 入口的前提下，建议将内部实现拆成以下模块。模块之间只通过 typed 对象通信，避免在模块间传递未校验的字典。

```text
src/
├── agent.py                 # 官方 reset/respond 适配
├── config.py                # AgentConfig、版本和默认值
├── state/
│   ├── models.py             # SessionState、SlotValue、ParsedTurn
│   ├── store.py              # 会话生命周期和 reset
│   └── overrides.py          # 覆盖、清除、否定、TTL
├── nlu/
│   ├── router.py             # Buying/Browsing 路由
│   ├── rules.py              # 规则解析和归一化
│   ├── structured.py         # 可选小模型/LLM 结构化抽取
│   └── schema.py             # 输入输出校验
├── catalog/
│   ├── loader.py             # 目录读取、字段映射、checksum
│   ├── normalize.py          # 文本、价格、类别、属性归一化
│   └── store.py              # ProductStore、字段倒排和元数据索引
├── retrieval/
│   ├── bm25.py               # 词法索引
│   ├── dense.py              # embedding 和向量索引
│   ├── filters.py            # 硬过滤和渐进放宽
│   └── hybrid.py             # 双轨召回、RRF、去重
├── ranking/
│   ├── features.py           # 候选特征计算
│   ├── rules.py              # 本地确定性排序
│   ├── reranker.py           # Cross-Encoder/LLM 可选适配
│   └── verifier.py           # 最终硬约束校验
├── dialogue/
│   ├── policy.py             # 推荐/澄清/放宽决策
│   ├── information_gain.py   # 槽位价值估计
│   └── templates.py          # 澄清问题模板
├── observability/
│   ├── trace.py              # JSONL 事件
│   └── replay.py             # 固定会话回放
└── scripts/
    ├── build_index.py
    ├── evaluate.py
    └── inspect_session.py
```

模块职责边界：`nlu` 只解释消息，`state` 只维护状态，`retrieval` 只产生候选，`ranking` 只排序和校验，`dialogue` 只决定下一步动作，`agent.py` 只负责编排和官方协议适配。这样可以对任意一层做 ablation，而不影响其它层。

## 23. 内部接口契约

以下接口是建议的内部稳定边界；字段名可按官方 kit 调整，但语义应保持一致。

```python
class IntentRouter(Protocol):
    def route(self, message: str, state: SessionState) -> IntentResult: ...

class SlotExtractor(Protocol):
    def extract(self, message: str, state: SessionState) -> ParsedTurn: ...

class Retriever(Protocol):
    def retrieve(self, state: SessionState) -> list[Candidate]: ...

class Reranker(Protocol):
    def rerank(self, query: str, candidates: list[Candidate], state: SessionState) -> list[Candidate]: ...

class DialoguePolicy(Protocol):
    def decide(self, state: SessionState, candidates: list[Candidate]) -> PolicyDecision: ...
```

建议定义以下对象，避免使用裸 `tuple`：

```python
@dataclass
class Candidate:
    parent_asin: str
    retrieval_scores: dict[str, float]
    features: dict[str, float]
    final_score: float = 0.0
    hard_valid: bool = True
    reasons: list[str] = field(default_factory=list)

@dataclass
class IntentResult:
    label: Literal["buying", "browsing", "unknown"]
    confidence: float
    explicit: bool
    evidence: list[str]

@dataclass
class PolicyDecision:
    action: Literal["recommend", "clarify", "relax", "fallback"]
    slot: str | None
    reason: str
    confidence: float
```

每个实现必须满足：输入不可变或在函数内复制；输出可序列化；不直接访问官方 evaluator 的全局变量；失败时抛出带 `error_code` 的受控异常。

## 24. 目录字段与归一化规则

### 24.1 字段优先级

推荐字段优先级如下：

1. `parent_asin`：唯一标识，缺失或重复时构建阶段失败。
2. `title`：检索和展示的最高权重字段。
3. `category`：过滤、类别分桶和澄清选项来源。
4. `brand/store`：品牌匹配和 Buying 硬约束。
5. `price`：预算过滤；无法解析的价格不得假设为 0。
6. `features`、`description`：语义补充，需截断和清洗。

### 24.2 文本归一化

- Unicode 规范化、大小写折叠、连续空白压缩。
- `$100`、`100 dollars`、`USD 100` 统一为数值 100。
- 颜色、尺码、鞋码和常见品类建立小型同义词表，例如 `sneaker/running shoe` 只做检索扩展，不自动当作严格同义。
- 品牌保留原始显示值，同时建立大小写不敏感的规范 key。
- HTML、控制字符和过长描述去除；不得删除标题中的型号和数字。

### 24.3 缺失值策略

- 缺失 `price`：不满足显式预算硬约束，但可以出现在无预算 Browsing 结果中。
- 缺失 `size`：不能声称“符合尺码”；若目录没有尺码字段，进入解释中的“信息不可用”，不要伪造。
- 缺失 `category`：依赖标题和向量召回，降低 category 特征权重。
- 缺失 `brand`：只有在用户未指定品牌时参与常规排序。

## 25. 槽位合并的确定性算法

### 25.1 更新优先级

同一轮多个来源冲突时，优先级固定为：

```text
用户明确的新值 > 用户明确清除 > 用户隐含新值 > 模型推断 > 旧状态
```

模型推断不得覆盖 `user-confirmed` 的硬槽位。每次变更写入 `slot_history`，至少保留旧值、新值、原因和轮次。

### 25.2 合并伪代码

```python
def merge_slot(old: SlotValue | None, new: SlotValue | None,
               clear: bool, explicit_override: bool) -> SlotValue | None:
    if clear:
        return None
    if new is None:
        return old
    if old is None:
        return new
    if old.kind == SlotKind.HARD and old.source == "user-confirmed":
        if not explicit_override and new.source != "user-confirmed":
            return old
    if explicit_override or new.confidence >= old.confidence:
        return new
    return old
```

### 25.3 多值和排除条件

- `color`、`brand` 在用户说“black or white”时可保存为允许集合。
- “不要红色”必须进入 `exclusions.color={red}`，不能仅把 `color` 设为空。
- 新的允许集合与排除集合冲突时，排除优先；若集合变为空，触发 `RELAX` 或 `CLARIFY`。
- `price_min > price_max`、尺码范围反转等矛盾状态不得进入检索，先请求用户确认。

### 25.4 TTL 规则

`HARD` 槽位无 TTL；`SOFT` 槽位可在 2～3 轮后降低排序权重；`CONTEXT` 槽位只在当前 query 和候选解释中生效。TTL 只能降低权重，不能删除用户明确说过的硬约束。

## 26. 过滤、空集和渐进放宽

### 26.1 过滤顺序

Buying 的默认过滤顺序：

```text
合法 ASIN → 明确排除 → 价格 → 尺码 → 品类 → 明确品牌 → 颜色/材质
```

实际顺序可按公开集调整，但每个过滤器必须单独记录命中数。这样能定位“目标被哪一个约束排除”。

### 26.2 空集处理

```python
def retrieve_with_relaxation(state):
    filters = build_hard_filters(state)
    candidates, report = filter_catalog(filters)
    if candidates:
        return search(candidates, state)

    for level in ["brand", "color_material", "category_synonym"]:
        relaxed = relax_one(filters, level)
        candidates, report = filter_catalog(relaxed)
        trace.record_relaxation(level, report)
        if candidates:
            state.relaxation_level += 1
            return search(candidates, state)

    # 预算和明确排除默认不自动放宽
    return []
```

如果只能通过放宽预算才能产生候选，应询问用户是否接受更高预算，而不是静默返回超预算商品。若 evaluator 的协议不允许纯澄清响应，则返回最接近的合法候选并在文本中明确“预算可能需要调整”。

### 26.3 过滤报告

每轮保存：初始目录数、每个过滤器前后数量、最终候选数、放宽级别。该报告只用于开发日志，不直接暴露给用户。

## 27. 候选特征、校准与多样性

### 27.1 特征表

每个候选的特征至少包括：

```text
lexical_norm, dense_norm, category_match, brand_match,
price_distance, color_match, size_match, soft_match,
exclusion_conflict, duplicate_group, source_coverage
```

`price_distance` 应使用相对距离，例如 `abs(price-budget)/max(budget, 1)`，避免高价商品的绝对差值支配排序。

### 27.2 分数校准

BM25、dense、Cross-Encoder 的分布不同，禁止直接把原始分数相加。推荐顺序：

1. 对每路结果取 top-k 后做 rank 或 percentile 归一化。
2. 在开发集上检查每路分数与目标命中的相关性。
3. 固定归一化方式和参数，写入 `config_version`。
4. 对空路由（例如 dense 关闭）重新归一化剩余权重，使总权重为 1。

### 27.3 Browsing 多样性

Browsing 最终 Top 10 可采用 MMR：

```text
MMR(d) = λ * relevance(d) - (1-λ) * max_{e∈selected} similarity(d,e)
```

`similarity` 可使用类别、品牌或标题 token overlap。只对同一商品族或高度相似标题施加惩罚，避免为了“多样性”排除真正相关的目标商品。Buying 默认不启用强多样性惩罚，以保护 MRR。

## 28. 对话策略决策表

| 条件 | 动作 | 说明 |
|---|---|---|
| `turn >= 10` | 推荐 | 强制结束，不能再问 |
| 空集且存在可安全放宽项 | 放宽后重检索 | 一次只放宽一个层级 |
| 空集且仅预算/排除冲突 | 澄清 | 询问是否调整冲突条件 |
| `count <= 10` 且 verifier 通过 | 推荐 | 直接给出候选 |
| `count > 100` 且缺失高价值槽位 | 澄清 | 问候选缩减最大的槽位 |
| Top1 margin 低且已问过主要槽位 | 推荐或保守澄清 | 晚轮优先推荐，避免 MTTC 恶化 |
| 解析失败但有旧候选 | 推荐旧候选 | 记录 fallback，不清空状态 |
| 解析失败且无候选 | 澄清 | 使用通用但具体的类别问题 |

每轮策略必须产生 `reason`，例如 `high_entropy_missing_category`、`empty_after_budget_filter`，方便错误分析和 Demo 展示。

## 29. 缓存、并发假设与性能预算

### 29.1 缓存层级

```text
L1: 归一化 query → ParsedTurn
L2: (route, query, filter_signature) → candidate IDs
L3: (query, parent_asin, reranker_version) → rerank score
L4: clarification slot → rendered template
```

缓存 key 必须包含模型、目录和配置版本；改变槽位或硬过滤签名后不得复用旧候选。单用户隔离且无并发是题目允许假设，若未来并发，`SessionStateStore` 需改为显式 session key + 线程安全容器。

### 29.2 内存估算方法

启动时输出：商品数、词表大小、embedding 维度、向量矩阵字节数、索引对象估算大小。向量矩阵内存约为：

```text
num_products × embedding_dim × bytes_per_float
```

例如 50,000 × 384 × 4 bytes 约 73 MB，实际还需加模型和索引开销。若超出预算，优先降低 embedding 维度或使用 float16（需验证召回变化），不要在运行时分批丢弃目录。

### 29.3 延迟预算

建议在本地先建立以下目标，再用真实设备校准：

| 阶段 | 目标 p50 | 失败策略 |
|---|---:|---|
| 规则解析 | < 5 ms | 直接 fallback |
| BM25 + 元数据过滤 | < 50 ms | 降低 k |
| Dense 检索 | < 150 ms | 关闭 dense |
| Top 50 本地重排 | < 100 ms | 只用规则 |
| 外部 LLM | < 600 ms | 超时回退 |

不要为了单轮延迟牺牲召回；最终以 evaluator 和 p95 延迟共同决定配置。

## 30. 错误码、降级和恢复

统一错误码便于 trace 聚合：

| 错误码 | 触发 | 恢复 |
|---|---|---|
| `E_INPUT_TYPE` | 消息为空或类型错误 | 使用空 query fallback |
| `E_PARSE_SCHEMA` | 结构化抽取字段非法 | 使用规则结果 |
| `E_INDEX_NOT_READY` | 索引未加载 | 使用 starter BM25 或安全错误响应 |
| `E_MODEL_TIMEOUT` | embedding/reranker/LLM 超时 | 关闭该层并继续 |
| `E_MODEL_OUTPUT` | 未知 ASIN、重复或非法 JSON | 本地排序 |
| `E_EMPTY_RESULT` | 硬过滤后无商品 | 记录过滤报告并澄清/放宽 |
| `E_PROTOCOL` | 官方响应转换失败 | 返回合法空候选/协议允许的 fallback |
| `E_TURN_LIMIT` | 达到第 10 轮 | 强制推荐并记录 |

恢复动作必须是单调的：一轮内从高成本组件退回低成本组件，不在同一轮反复尝试同一个失败服务。

## 31. 测试用例矩阵

| 类别 | 输入序列 | 期望断言 |
|---|---|---|
| 首轮 Buying | `black running shoes under $100` | 路由 buying，价格/颜色/品类写入，硬过滤生效 |
| 首轮 Browsing | `what to wear to a summer wedding` | 路由 browsing，保留场景，结果具备多样性 |
| 增量槽位 | `shoes` → `under $100` → `size 9` | 槽位逐步增加，查询每轮更新 |
| 覆盖 | `black` → `actually white` | 只保留 white，旧值不参与过滤 |
| 清除 | `Nike` → `any brand is fine` | brand 为空，来源记录为 user override |
| 否定 | `red shoes` → `not red` | red 进入 exclusions 或被清除 |
| 冲突预算 | `under $100` → `between $150 and $200` | 新预算覆盖旧预算，必要时标记 hard override |
| 空集 | `size 99` | 不崩溃，按策略澄清或安全放宽 |
| 模型失败 | LLM 非法 JSON | 本地解析和排序仍返回结果 |
| 第 10 轮 | 连续 10 条消息 | 第 10 轮推荐，第 11 轮不被调用 |
| 会话隔离 | session A 设置 Nike，reset，session B | B 不包含 Nike 或 A 的候选 |
| 合法性 | 目录含重复候选 | 输出去重、最多 10 个有效 ASIN |

## 32. 实验配置与验收门槛

### 32.1 实验记录模板

```markdown
## Experiment EXP-YYYYMMDD-NN

- Hypothesis:
- Code commit:
- Catalog checksum:
- Evaluator version:
- Config version:
- Changed component:
- Public split:
- Hit@10:
- MRR:
- MTTC:
- Efficiency:
- TechnicalScore:
- p50/p95 latency:
- Memory peak:
- Failure breakdown:
- Conclusion:
- Next action:
```

### 32.2 阶段门槛

- **MVP 门槛**：官方接口通过、200 公共会话可跑完、无 session 泄漏、所有输出合法。
- **双轨门槛**：Hybrid 相比 BM25 的召回和/或 MRR 有可重复提升，且 p95 延迟在预算内。
- **对话门槛**：Override、否定、空集和第 10 轮测试通过；MTTC 不因澄清显著恶化。
- **增强门槛**：Cross-Encoder/LLM 只有在独立验证切分上改善目标指标才启用，否则保留为关闭开关。
- **发布门槛**：干净环境按 README 完成安装、索引构建和 evaluator，结果与记录在允许误差内一致。

## 33. 开发顺序与提交粒度

建议按可回滚的小提交推进：

```text
1. preserve_official_adapter
2. add_catalog_normalization
3. add_session_state_and_reset
4. add_rule_intent_and_slots
5. add_bm25_retrieval
6. add_hard_filter_and_validation
7. add_dense_index (optional)
8. add_rrf_fusion
9. add_override_and_negation
10. add_local_reranker
11. add_dialogue_policy
12. add_information_gain_clarification
13. add_trace_and_replay
14. add_ablation_configs
15. freeze_final_candidate
```

每个提交应能独立运行最小测试；实验型提交使用 `experiment/*` 分支，不把未经验证的外部服务绑定到主分支。

## 34. 运行手册

### 34.1 首次构建

```text
安装官方依赖
→ 放置并校验目录
→ 运行 build_index.py
→ 检查 index manifest
→ 用单条 query 做 smoke test
→ 运行一小批 public sessions
```

`index manifest` 至少包含目录 checksum、记录数、模型名称、向量维度、归一化版本和构建时间。

### 34.2 运行前检查

- `dense_enabled=true` 时模型文件可读且维度与索引一致。
- 目录记录数和唯一 `parent_asin` 数量符合预期。
- `max_turns <= 10`，`rerank_k <= fused_k`，所有权重非负。
- 外部 LLM 配置缺失时自动关闭，不在启动阶段阻塞。

### 34.3 故障排查顺序

```text
协议错误 → 检查 adapter/schema
全量空结果 → 检查目录字段与 filter
命中率下降 → 先看 retrieval_miss，再看 hard_filter_miss
MRR 下降 → 检查分数归一化和 reranker
MTTC 上升 → 检查澄清阈值、重复提问和末轮保护
延迟上升 → 查看 trace 中各阶段耗时和缓存命中率
```

## 35. 技术文档维护规则

当修改状态、槽位、索引、权重或官方适配器时，必须同步更新：

1. 本文对应章节和配置示例。
2. 单元/集成测试矩阵。
3. `config_version` 和实验记录。
4. README 的运行与复现命令。

任何“只改代码不改文档”的行为都会使实验结果无法解释。最终提交前，以冻结的代码、配置、目录 checksum 和本文档共同构成可复现版本。
