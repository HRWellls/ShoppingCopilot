# Shopping Copilot 开发者交付总结

本次交付完成了开发者行动清单对应的运行自检、基线记录、离线 fallback 入口、可视化演示和操作说明。核心 Agent API、状态隔离、Top-10 去重与既有检索逻辑保持兼容。

## 已交付内容

- `scripts/delivery_self_check.py`：检查样例目录、checksum、配置版本以及 dense/LLM/BM25 可选能力。
- `scripts/capture_baseline.py`：将 Python、平台、Git revision、目录 checksum、配置快照、路由和启动耗时写入 `.runtime/baseline.json`；不包含 API key 或私有标签。
- `demo/app.py` + `demo/index.html`：本地浏览器演示，展示模拟用户/Agent 多轮对话、每轮 Top-10 商品、目标商品高亮、结果数量和 offline BM25 状态。
- OpenSpec 变更规划：`openspec/changes/complete-developer-delivery-demo/`。
- `.gitignore` 已排除 `.runtime/`、模型缓存、密钥和私有结果，避免运行产物误提交。

## 快速操作

```powershell
cd "D:\tiktok tech jam2026\Hamburgerr"
python -m scripts.delivery_self_check --json
python -m scripts.capture_baseline --output .runtime/baseline.json
python -m unittest tests.test_agent tests.test_config_models tests.test_sample_catalog -q
python -m demo.app
```

启动 Demo 后打开 <http://127.0.0.1:8765>，使用 `Next turn` 查看下一轮，`Replay` 重新生成一段会话。没有 API key、网络或本地 dense 模型时仍可运行，页面会显示 `Offline BM25 fallback`。

## 评估与排障

完整公开集评估：

```powershell
python -m scripts.phase2_benchmark --output .runtime/phase2-validation.json
python -m scripts.phase3_benchmark --variant full --output .runtime/phase3-full.json
```

样例目录缺失时先准备 `data/catalog_sample_100.jsonl` 或官方 `data/catalog.jsonl`。若自检报告 required catalog 失败，修复路径/文件后重试；optional 能力失败不会阻止 BM25 路径。运行产物只保存在 `.runtime/`，回滚时删除新增脚本和 Demo 文件即可，不影响原 Agent API。

## 验证记录

已验证：18 个核心 Agent/配置/样例目录单元测试通过；样例目录 100 条记录可加载；基线 JSON 成功生成；自检 required 项通过。完整测试发现仓库中已有的全量评估用例运行时间较长，建议在具备完整目录的环境单独执行。
