# Shopping Copilot 产品文档

## 1. 文档概述

### 1.1 产品名称

Shopping Copilot：面向服饰、鞋类和珠宝目录的多轮对话式购物检索 Agent。

### 1.2 产品定位

Shopping Copilot 不是一个展示型电商前端，而是一个可被评测器调用的后端 Agent。它把用户的自然语言需求转化为结构化购物状态，在 50,000 个 Amazon 商品中逐步缩小候选集，并在“继续检索、主动澄清、直接推荐”之间做出可解释的决策。

产品的核心承诺是：

> 找到正确商品，把它排在前面，只提出值得用户回答的问题。

### 1.3 目标用户

- 有明确购买目标、希望快速收敛的高意图用户（Buying）。
- 仍在探索搭配、场景或品类，希望获得多样选择的低意图用户（Browsing）。
- 在对话中会补充、否定或改写条件的用户。

### 1.4 产品边界

**包含：**文本意图识别、槽位抽取与更新、Buying/Browsing 双轨检索、候选排序、主动澄清、会话状态管理、评测和运行轨迹记录。

**不包含：**网页 UI、支付和库存交易、目录写入、跨用户实时协作、多模态输入、基础模型全参数训练、外部向量数据库集群。

## 2. 用户问题与机会

传统关键词搜索有三个缺口：

1. 用户的需求往往不完整，首句只表达“想找什么”，没有给出预算、尺码或场景。
2. 用户会在后续轮次修改先前条件；简单拼接历史会把旧值和新值同时保留。
3. 浏览和购买是不同任务。购买需要严格满足硬约束，浏览需要语义扩展和品类多样性。

因此，产品必须把搜索看成一个有状态的决策过程，而不是一次性 query-to-list。

## 3. 核心用户旅程

### 3.1 Buying：高精度购买

示例：

```text
用户：I need black running shoes under $100.
Agent：先识别购买意图，提取品类、颜色和最高预算，执行硬过滤并返回候选。
用户：Nike only.
Agent：覆盖 brand 槽位，重新检索和重排。
用户：Actually any brand is fine.
Agent：清除 brand 槽位，不再把 Nike 当成硬条件，返回更新后的 Top 10。
```

体验原则：精确优先、硬约束不可违反、尽量在较少轮次内给出可用结果。

### 3.2 Browsing：探索与灵感

示例：

```text
用户：What should I wear for a summer wedding?
Agent：识别浏览意图，把场景、季节和正式程度转成语义查询，提供跨品类且有差异的候选。
用户：Show me something less formal.
Agent：降低 formal 场景偏好权重，保留夏季婚礼上下文并重新排序。
```

体验原则：语义理解优先，避免过早硬过滤，结果之间保持品类或风格多样性。

### 3.3 信息不足时的主动澄清

当候选空间过大、候选熵高或排名置信度不足时，Agent 不返回随意的 Top 10，而是提出一个结构化、可直接回答的问题：

```text
用户：I want some shoes.
Agent：Are you looking for running, casual, or formal shoes?
```

问题选择依据是预期候选缩减量与增加一轮对话成本的比值。每轮最多询问一个属性，并同时保留当前候选或搜索摘要，避免用户感觉对话被“卡住”。

## 4. 产品能力

### 4.1 意图路由

将每轮输入路由为 `buying` 或 `browsing`，并输出置信度。显式购买信号（预算、尺码、明确购买动词、具体品牌等）优先进入 Buying；场景、搭配、灵感和“推荐一些选择”等表达优先进入 Browsing。低置信度时沿用上一轮路由，只有明确的意图覆盖才切换轨道。

### 4.2 需求槽位与记忆

产品维护当前会话目标，而不是无限累积原始文本。首版槽位包括：

`category`、`brand`、`price_min`、`price_max`、`color`、`size`、`material`、`occasion`、`gender`、`style`、`exclusions`。

每个槽位有值、类型（硬约束/软偏好/上下文）、置信度、来源和最近出现轮次。用户明确说“换成、其实、任何品牌都可以、不要……”时，系统执行覆盖、删除或重写。

### 4.3 双轨混合检索

- Buying：先应用预算、尺码、品类等硬过滤，再以词法匹配为主、语义召回为辅。
- Browsing：以 dense 语义召回为主，辅以 BM25、品类和元数据信号，不做过硬过滤。
- 两条轨道都经过候选融合、去重、重排和硬约束校验。

### 4.4 可控的候选解释

每个推荐项至少返回 `parent_asin`，并可附带标题、品类、价格和命中原因。解释只使用目录中的事实，不让模型臆造库存、评价或功能。

### 4.5 失败与降级

任一可选模型超时、输出格式不合法或索引不可用时，系统必须退回规则抽取、BM25 和确定性排序。第 10 轮必须返回候选，不允许继续追问；任何情况下不产生第 11 轮。

## 5. 交互与响应规范

### 5.1 每轮处理流程

```text
接收消息 → 解析意图和槽位 → 处理覆盖/否定 → 选择检索轨道
→ 召回与融合 → 重排与校验 → 推荐或澄清 → 记录轨迹
```

### 5.2 推荐响应

推荐响应应简短、可扫描，包含：

- 当前理解的一句话摘要（可选）。
- 最多 10 个去重后的 `parent_asin`。
- 需要展示时附带标题、价格和匹配理由。
- 若仍有不确定性，附带一个下一步可回答的问题，但不能用问题替代候选返回（除非协议明确要求纯文本澄清）。

### 5.3 澄清响应

澄清问题必须：

- 指向一个具体缺失槽位。
- 给出 2～4 个自然选项或示例。
- 避免“请提供更多信息”这类低信息量句式。
- 不重复询问用户已明确回答过的属性。

### 5.4 会话限制

- 单会话最多 10 轮。
- 会话彼此隔离，`reset` 后不得残留上一会话状态。
- 目录只读，推荐结果必须来自有效目录记录。

## 6. 产品状态机

```text
START
  ↓
UNDERSTAND（意图/槽位解析）
  ↓
RETRIEVE（Buying 或 Browsing）
  ↓
ASSESS（候选规模、熵、置信度）
  ├─ 候选稳定 → RECOMMEND
  ├─ 候选过宽 → CLARIFY
  └─ 违反硬约束/空集 → RELAX_OR_CLARIFY
RECOMMEND / CLARIFY
  ↓
WAIT_NEXT_TURN
  └─ 新消息含 override → REWRITE_STATE → RETRIEVE
```

状态机是产品行为的稳定边界；模型只负责受约束的理解和排序，不直接决定任意工具调用或 ASIN。

## 7. 目标与指标

### 7.1 业务目标

1. 提升真实购买商品进入 Top 10 的概率（Coverage）。
2. 将真实购买商品排到更靠前位置（Precision）。
3. 在不损失命中率的情况下减少平均对话轮数（Efficiency）。

### 7.2 评测指标

- **Hit Rate@10**：真实购买商品是否出现在某轮 Top 10。
- **MRR**：真实商品排名为 `r` 时贡献 `1/r`，越靠前越好。
- **MTTC**：平均转化所需轮数；不必要的澄清会使其上升。
- **Efficiency / TechnicalScore**：以官方 evaluator 的定义为准，统一记录并报告。

题目给出的核心组合目标为 `0.50 × HitRate@10 + 0.30 × MRR + 0.20 × Efficiency`；实现和报告以官方 evaluator 的实际输出为最终依据。

### 7.3 产品验收标准

- Buying/Browsing 在公开会话中可区分，低置信度有稳定 fallback。
- 槽位可新增、覆盖、删除，否定条件不会被旧值抵消。
- 每轮最多返回 10 个合法且去重的 `parent_asin`。
- 候选过宽时提出高信息量澄清，候选稳定时直接推荐。
- 第 10 轮仍能完成合法响应；无状态泄漏、无未捕获异常。
- 所有关键行为和指标可由日志与离线回放复现。

## 8. 价值主张与创新叙事

Shopping Copilot 的差异不在于“接入了某个大模型”，而在于运行时持续回答三个问题：

1. 现在应该搜什么？（意图路由和状态）
2. 应该如何搜？（Buying/Browsing 双轨混合检索）
3. 下一轮最值得问什么？（信息增益澄清）

这使产品同时覆盖搜索召回、结果排序和对话效率，适合在 Devpost、Demo 和答辩中以“自适应对话式检索 Agent”定位。

## 9. 迭代路线

### MVP

官方 `reset/respond` 接口、目录预处理、规则意图、BM25、基础槽位状态、Top 10 输出和 evaluator 记录。

### V1：双轨检索

加入 dense 向量、RRF/加权融合、Buying 硬过滤与空集渐进放宽、Browsing 多样性。

### V2：对话收敛

加入结构化抽取、Intent Override、候选熵和信息增益问题选择、10 轮保护。

### V3：精度保护

对 Top 30～50 做 Cross-Encoder 或可选低温 LLM 重排；低置信度最多启用一次 Verifier，失败即降级。

### V4：实验与发布

按 Buying、Browsing、Override、Boundary 分层评估，冻结配置和索引版本，完成 README、公开仓库、Demo 和 Devpost 材料。

## 10. 风险与用户影响

| 风险 | 用户影响 | 产品对策 |
|---|---|---|
| 过滤过硬 | 正确商品被排除 | 空集检测、按优先级渐进放宽 |
| 追问过多 | MTTC 上升、体验中断 | 信息增益阈值、每轮一个问题、末轮保护 |
| 语义偏移 | 结果看似相关但不符合购买条件 | 标题/品类加权、硬约束校验 |
| 模型不可用 | 响应失败或延迟 | 规则和本地排序 fallback |
| 公共集过拟合 | 私有集表现下降 | 分层验证、固定实验记录、禁止 case-specific 规则 |

## 11. 发布与演示要求

最终产品以 headless Agent 交付。演示应覆盖：一段 Buying 多轮、一次条件覆盖、一次 Browsing 场景、一次主动澄清，以及 evaluator 指标。公开仓库 README 需包含安装、运行、复现实验、限制、未来工作和团队贡献；不得提交 API key、私有评测数据或不可恢复的模型缓存。

