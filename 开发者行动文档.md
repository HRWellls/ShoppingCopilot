# 开发者行动文档

## 目标

在不破坏当前 Hit Rate@10、MRR、MTTC 基线的前提下，证明 Shopping Copilot 是可靠、可复现、可降级、可扩展的工程系统。

当前公开集基线：Hit Rate@10 `0.9600`、MRR `0.606629`、MTTC `2.585`、TechnicalScore `0.830289`、p95 `68.675 ms`。除非发现正确性或稳定性问题，最后阶段冻结检索权重。

## 工作顺序

### 1. 冻结基线

- 记录当前分支、commit、Python 版本、依赖版本、配置版本、目录 checksum 和索引 manifest。
- 运行公开集评测，保存原始 JSON 和简短摘要到 `.runtime/`，不要提交私有标签、API key 或模型缓存。
- 标注当前启用的路径：规则、BM25、dense、LLM、clarification、relaxation。

**交付物：**`baseline` 结果 JSON、配置快照、复现命令。

### 2. 验证协议和会话隔离

- 运行完整单元、集成、evaluator 和 catalog smoke tests。
- 测试重复 `reset`、未知 session、非法输入、空消息和异常组件。
- 检查每轮最多 10 个合法且去重的 `parent_asin`，第 10 轮不再追问。
- 验证会话 A 的槽位、候选和历史不会泄漏到会话 B。

**验收标准：**contract failure = 0，session leakage = 0，所有异常均返回协议兼容响应。

### 3. 完善离线 fallback 和启动自检

- 无 API key、无网络、无本地 dense 模型时，确认规则 + BM25 路径可独立完成响应。
- 对目录不存在、索引损坏、manifest 不匹配、依赖缺失提供可读错误。
- 外部模型超时、HTTP 错误、非法 JSON、未知字段和缺少 usage 时，自动回退且不暴露堆栈。
- 增加一次启动自检命令，输出依赖、目录、索引、配置和可选能力状态。

**交付物：**启动自检脚本、fallback 测试、现场运行说明。

### 4. 补齐边界与鲁棒性测试

至少覆盖：

- 价格上下限、价格缺失、预算冲突；
- 品牌/颜色/材质的否定和清除；
- `Nike -> any brand`、`black -> white` 等 override；
- 同义表达、拼写错误、中英文混合和空结果；
- Buying/Browsing 切换与低置信度路由；
- 重复消息、乱序 turn、turn=10 和 top_k 异常值。

每个测试应说明输入、预期槽位状态、检索动作和响应约束，而不仅是最终 ASIN。

### 5. 产出工程指标

统一在同一环境测量并记录：

- 初始化时间；
- 单轮 p50/p95 延迟；
- 20～100 个连续会话的错误率；
- 内存峰值；
- dense 索引构建时间和文件大小；
- LLM 调用次数、超时率和 token 成本（启用时）。

不要将不同实验包装器的延迟直接横向比较；报告环境和配置。

### 6. 分层评测和 ablation

- 按 Buying、Browsing、Intent Override、Boundary 输出 Hit@10、MRR、MTTC。
- 至少保留 full、no-clarification、no-relaxation、no-dense/LLM fallback 对照。
- 额外准备不参与调参的自建盲测集，避免只报告公开集最优结果。
- 解释每个模块改善的是召回、排序、轮数还是稳定性。

**交付物：**一页分层表、一页 ablation 图或表、盲测集说明。

### 7. 整理可复现交付

- README 提供安装、数据准备、索引构建、评测和 Demo 命令。
- 配置集中管理，记录版本号和回滚方式。
- 确认 `.gitignore` 排除 `.runtime`、模型缓存、密钥和私有结果。
- 在干净环境执行一次从安装到评测的完整流程。

## 开发者交付清单

- [ ] 基线结果和配置已冻结。
- [ ] 全部测试通过，协议和隔离检查为 0 失败。
- [ ] 无 API/无 dense 时 fallback 可运行。
- [ ] 启动自检和异常降级已验证。
- [ ] 边界测试覆盖 override、否定、空集和末轮。
- [ ] 延迟、资源、成本数据已记录。
- [ ] 分层评测、ablation 和盲测结果已生成。
- [ ] README 和一键命令可在干净环境运行。
- [ ] 最终代码只保留必要修改，避免临近提交引入检索回归。

## 给产品/Demo 人员的输入

向产品负责人提供：架构图、四个场景的实际输出、指标摘要、fallback 演示、资源成本、系统限制和可公开的运行命令。所有数字必须来自冻结配置和可复现结果。
